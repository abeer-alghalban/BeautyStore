<!DOCTYPE html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>لوحة التحكم - مركز التجميل</title>
    <link rel="stylesheet" href="~/css/admin.css" />
  </head>
  <body>
    <div class="admin-container">
      <aside class="sidebar">
        <div>
          <img src="~/Img/logo.png" alt="شعار الموقع " class="logo" />
        </div>

        <nav>
          <ul>
            <li class="active">
              <a href="~/admin.html"><span>📊</span> التقارير</a>
            </li>
            <li>
              <a href="~/products.html"><span>💄</span> المنتجات</a>
            </li>
            <li>
              <a href="~/add-product.html"><span>➕</span> إضافة منتج</a>
            </li>
            <li>
              <a asp-controller="Admin" asp-action="Orders"><span>🛒</span>الطلبات</a>
            </li>
          </ul>
        </nav>
      </aside>

      <main class="main-content">
        <!-- شريط العنوان -->
        <header class="content-header">
          <h1>لوحة التحكم</h1>
          <div class="admin-actions">
            <span class="admin-name">مرحباً، آدمن</span>
          <a asp-controller="Account" asp-action="Logout">تسجيل خروج</a>
          </div>
        </header>

        <!-- الأقسام الرئيسية -->
        <section class="dashboard-sections">
          <h3>التقارير</h3>

          <!-- قسم التقارير -->
          <div class="charts">
            <div class="chart-container">
              <canvas id="salesChart"></canvas>
            </div>
            <div class="chart-container">
              <canvas id="popularProductsChart"></canvas>
            </div>
          </div>

          <!-- قسم المنتجات -->
          <div class="products-section">
            <div class="section-header">
              <h2>المنتجات</h2>
              <a href="~/add-product.html" class="add-product-btn">
                إضافة منتج جديد
              </a>
            </div>

            <div class="products-list" id="products-list"></div>
          </div>
        </section>
      </main>
    </div>

    <!-- JavaScript Files -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="~/js/admin-charts.js"></script>
    <script src="~/js/admin.js"></script>
  </body>
</html>
