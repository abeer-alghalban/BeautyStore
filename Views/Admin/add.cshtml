<!DOCTYPE html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>إضافة منتج - مركز التجميل</title>
    <!-- Font Awesome for icons -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />

    <link rel="stylesheet" href="~/css/admin.css" />
    <link rel="stylesheet" href="~/css/add-product.css" />
  </head>

  <body>
    <div class="admin-container">
      <aside class="sidebar">
        <div>
          <img src="Img/logo.png" alt="شعار الموقع " class="logo" />
        </div>

        <nav>
          <ul>
            <li>
              <a href="admin.html"><span>📊</span> التقارير</a>
            </li>
            <li>
              <a href="products.html"><span>💄</span> المنتجات</a>
            </li>
            <li class="active">
              <a href="add-product.html"><span>➕</span> إضافة منتج</a>
            </li>
          </ul>
        </nav>
      </aside>

      <main class="main-content">
        <header class="content-header">
          <h1 id="page-title">إضافة منتج جديد</h1>
          <div class="admin-actions">
            <span class="admin-name">مرحباً، آدمن</span>
            <a href="index.html" class="logout-btn">تسجيل خروج</a>
          </div>
        </header>

        <section class="form-section">
          <form id="product-form">
            <div class="form-group">
              <label for="product-name">اسم المنتج</label>
              <input
                type="text"
                id="product-name"
                placeholder="أدخل اسم المنتج"
                required
              />
            </div>

            <div class="form-group">
              <label for="product-description">وصف المنتج</label>
              <textarea
                id="product-description"
                placeholder="أدخل وصف المنتج"
                rows="4"
              ></textarea>
            </div>

            <div class="form-group">
              <label for="product-price">السعر (شيكل)</label>
              <input
                type="number"
                id="product-price"
                placeholder="أدخل سعر المنتج"
                min="1"
                required
              />
            </div>

            <div class="form-group">
              <label for="product-category">الفئة</label>
              <select id="product-category" required>
                <option value="">اختر فئة المنتج</option>
                <option value="skin">العناية بالبشرة</option>
                <option value="hair">العناية بالشعر</option>
              </select>
            </div>

            <div class="form-group">
              <label for="product-image">صورة المنتج</label>
              <input type="file" id="product-image" accept="image/*" required />
              <div class="image-preview" id="image-preview">
                <i class="fas fa-image preview-icon"></i>
                <p>ستظهر الصورة هنا</p>
              </div>
            </div>

            <div class="form-actions">
              <button type="button" class="cancel-btn" onclick="cancelForm()">
                إلغاء
              </button>
              <button type="submit" class="submit-btn">حفظ المنتج</button>
            </div>
          </form>
        </section>
      </main>
    </div>

    <!-- JavaScript File -->
    <script src="~/js/add-product.js"></script>
  </body>
</html>
